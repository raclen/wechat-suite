# WeChat Daily - Python 依赖

# AI API（OpenAI 兼容格式，支持 DeepSeek / 通义千问 / OpenAI）
openai>=1.0.0
socksio>=1.0.0

# Notion API & 自动导出 HTTP 请求
requests>=2.31.0

# 配置管理
pyyaml>=6.0

# Markdown 转 PNG（Figma 风格截图）
playwright>=1.40.0

# ---------- 直接读取微信数据库（wechat_core）----------
# 数据库解密（必需）
cryptography>=41.0.0

# 微信进程检测（必需）
psutil>=5.9.0

# 版本号比较（密钥提取用）
packaging>=23.0

# zstd 压缩消息解码（可选，部分消息需要）
zstandard>=0.21.0

# 微信进程密钥提取（可选，首次获取密钥时需要）
# pip install wx_key
